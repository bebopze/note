
### 1.日志搜索

```
    less xx.log     类似用vim打开日志
    
    
    /:keyword       精准匹配keyword  (向后搜索)
    
    # 配合n/N 跳转匹配行：
    n               向后 跳转到下一个匹配行
    
    N               向前 跳转到上一个匹配行
    
    
    ?:keyword       精准匹配keyword  (向前搜索)       n/N 功能反向
    
    
    q               退出
```